{
  "name": "@nexu-design/tokens",
  "version": "1.0.1",
  "private": false,
  "description": "Design tokens, theme variables, and shared CSS for Nexu UI packages",
  "license": "UNLICENSED",
  "files": ["dist", "README.md"],
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./styles.css": "./dist/styles.css"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "generate:styles": "node ./scripts/generate-styles.mjs",
    "build": "pnpm run generate:styles && tsc -p tsconfig.build.json && cp src/styles.css dist/styles.css && cp src/token-source.json dist/token-source.json && mkdir -p dist/fonts && cp src/fonts/* dist/fonts/",
    "clean": "rm -rf dist",
    "pack:check": "pnpm run build && npm pack --dry-run",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
