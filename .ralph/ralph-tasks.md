# Ralph Tasks

Add your tasks below using: `ralph --add-task "description"`

- [x] Add `Alert` to `@nexu/ui-web` and migrate alert/callout surfaces in `ChannelsPage.tsx` and `AutomationPage.tsx`
- [x] Add `Progress` to `@nexu/ui-web` and migrate progress bars in `PostAuthSetupPage.tsx` and `NexuProgressPage.tsx`
- [x] Add `StatCard` to `@nexu/ui-web` and migrate KPI cards in `DashboardPage.tsx`, `BillingPage.tsx`, `NexuProgressPage.tsx`, `TeamPage.tsx`, and `AutomationPage.tsx`
- [x] Add `PricingCard` to `@nexu/ui-web` and migrate pricing surfaces in `BillingPage.tsx` and `PricingModal.tsx`
- [x] Add `Table` to `@nexu/ui-web` and migrate representative dense table views in `NexuApprovalsPage.tsx`, `NexuTaskPage.tsx`, `ChannelsPage.tsx`, `TeamTasks.tsx`, and `SessionsPage.tsx`
- [x] Add `ScrollArea`, `Accordion`, `Collapsible`, `PanelFooter`, `InteractiveRow`, and `DataTable` to `@nexu/ui-web` and migrate their Phase 2 target pages
- [x] Add `ConversationMessage`, `Stepper`, and `EntityCard` to `@nexu/ui-web` and migrate their Phase 3 target pages
- [x] Add `StatsBar` to `@nexu/ui-web` and migrate `TeamPage.tsx` and `StepTeam.tsx`
- [x] Audit and resolve the remaining partial migration notes for `NexuProgressPage.tsx` and `OnboardingPage.tsx`
- [x] Add `Toggle` / `ToggleGroup` to `@nexu/ui-web` and migrate the remaining toggle-style surfaces
- [ ] Add `ResizablePanel` / `SplitView`, `Sidebar` / `NavigationMenu`, `ActivityBar`, and `DetailPanel` to `@nexu/ui-web` and migrate the desktop shell surfaces
- [ ] Add `Combobox`, `Breadcrumb`, and optional `TagGroup` to `@nexu/ui-web` and migrate the remaining target surfaces
