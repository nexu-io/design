# Changesets

Use `pnpm changeset` to record a consumer-facing change for `@nexu-design/tokens`
or `@nexu-design/ui-web`.

When changesets land on `main`, the release workflow opens or updates a version PR.
Merging that PR publishes any unpublished package versions to npm.
