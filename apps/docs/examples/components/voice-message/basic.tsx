import { VoiceMessage } from "@nexu-design/ui-web";

export function VoiceMessageBasicExample() {
  return (
    <VoiceMessage
      duration="0:24"
      transcript="We sized the SLO at 99.5 so we have room before paging someone."
    />
  );
}
